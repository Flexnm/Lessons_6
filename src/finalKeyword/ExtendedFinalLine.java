package finalKeyword;

public class ExtendedFinalLine { //extends FinalLine { // Cannot extend 'final' class.
	
	public ExtendedFinalLine(int length) {
	//	super(length); // Error caused due to the 'final'.
	}

}
